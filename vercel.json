{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
                "runtime": "python3.12"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/app.py"
        }
    ],
    "installCommand": "apt-get update && apt-get install -y cmake build-essential libboost-all-dev python3.12-distutils && python3.12 -m pip install --upgrade pip && python3.12 -m pip install --no-cache-dir -r requirements.txt"
}